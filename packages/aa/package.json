{
  "name": "@squaresdk/aa",
  "version": "0.1.0",
  "description": "ERC-4337 account abstraction for Square agents on Arc: SimpleAccount v0.7 smart accounts and a self-bundler that submits UserOperations through EntryPoint.handleOps from a keeper EOA.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "measure": "tsx scripts/measure.ts",
    "measure:live": "tsx scripts/measure-live.ts"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
