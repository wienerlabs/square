[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 10_000
via_ir = false
bytecode_hash = "none"
cbor_metadata = false
fs_permissions = [{ access = "read-write", path = "./deployments" }, { access = "read", path = "./test/fixtures" }]
remappings = [
  "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
  "forge-std/=lib/forge-std/src/",
]

[profile.ci]
fuzz = { runs = 512 }
# No invariant setting here until there is an invariant test to configure.
# `invariant = { runs = 128, depth = 32 }` sat here and ran nothing: there is
# no invariant_ function under test/. Put it back with the first one.

[fmt]
line_length = 110
tab_width = 4
bracket_spacing = false
int_types = "long"

[lint]
exclude_lints = ["mixed-case-function", "mixed-case-variable", "incorrect-shift", "screaming-snake-case-immutable", "asm-keccak256"]

[rpc_endpoints]
arc_testnet = "${ARC_RPC_URL}"

[etherscan]
